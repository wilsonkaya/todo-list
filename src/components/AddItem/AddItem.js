import React from 'react'

const AddItem = ({addTodo}) => {
  // Input tracker
  let input;

  return (
    <div>
      <input />
      <button>
        +
      </button>
    </div>
  );
};

export default AddItem
